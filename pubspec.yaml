name: prototyp_events
description: Event Organisation

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  adobe_xd: ^2.0.0+1
  flutter_icons: ^1.1.0
  google_fonts: ^2.0.0

  firebase_core: ^1.6.0
  firebase_auth: ^3.1.0
  cloud_firestore: ^2.5.1
  firebase_database: ^7.2.1
  firebase_storage: ^10.0.3
  provider: ^6.0.0

  photo_view: ^0.12.0
  path: ^1.8.0
  webview_flutter: ^2.0.13
  cupertino_icons: ^1.0.2
  file_picker: ^4.0.2



dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.5.3

flutter:

  uses-material-design: true

  assets:
    - assets/images/banner.gif
    - assets/images/FinalWEBackground.jpg
    - assets/images/lucifer.png
    - assets/images/peacemaker.png
    - assets/images/profil.jpg
    - assets/images/raumplan.jpg
    - assets/images/secondDawn.png
    - assets/images/tickets.png

fonts:

